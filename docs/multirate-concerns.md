  1. ~~The "both unknown" heuristic in refine_with_types (rate_analysis.rs:232-241) reroutes cross-rate node-to-node edges with no graph-endpoint anchor and no explicit policy to EdgeKernel::Event { rescale: None }. The comment acknowledges this means stream edges in that exact configuration won't be resampled. Today's tests anchor at least one side; the safety net relies on test discipline. A future explicit [stream] policy keyword could let users assert intent.~~ **Resolved.** The both-unknown heuristic was deleted (commit `857de1d`); cross-rate stream edges now route through `CrossRateKernel` trait projection (where both endpoints are `EndpointAt`-derivable) or the concrete-kernel fallback (`SincUpFir`/`SincDownFir`), per `docs/cross-rate-projection-followup.md`. Regression test: `oscen-lib/tests/cross_rate_unanchored.rs::cross_rate_unanchored_stream_edge_resamples`.

  2. FanoutShape::classify_fanout silently truncates mismatched-array-size edges to Parallel { n: min(n, m) } (fanout.rs:33). Reasonable for parity with the existing same-rate path, but unsigned silent truncation in an audio graph feels like a future debugging trap. The docstring promotes hard-error to "future task" — fine to defer, but worth a tracking issue.

  3. String-based type matching for feedback nodes: is_feedback_allowing_node returns true for any node_type whose path's last segment contains "Delay" (codegen.rs:702-711). Pre-existing — not introduced here — but the multi-rate work doubles down on node_type lookups, so it's a good moment to consider promoting this to a marker trait (e.g., AllowsFeedback). **Deferred to its own design — see `docs/allows-feedback-refactor.md`.**

  4. voices array marker is identified by string-match on the field name (codegen.rs:211-247). Same shape as #3 — pre-existing, but multi-rate event handling now has to coexist with this convention. **Partially resolved in commit `059a930` ("Delete voices field-name kind-inference branch"): the kind-inference half is gone — `[EventOutput; N]` arrays are now classified as `EventArrayKind` via `derive(Node)`-emitted `EndpointAt` impls, no longer by field-name match. The per-voice loop emission half (codegen recognising the `voices` field for fan-out lowering) still uses the field-name convention; left as a follow-up.**

  5. Inner-node process_event_inputs() runs once per outer tick (codegen.rs:1769-1772). Combined with the Multiply(N) rescale of frame_offset, this means the consumer's gate handler must use frame_offset for sample-accurate timing inside its own process() — process_event_inputs() itself is not called per inner tick. Noted in the comment, but worth surfacing in user-facing docs since it's a subtle invariant

  6. ~~oscen-macros/Cargo.toml adds trybuild as a non-dev-dependencies dependency — Cargo.toml diff shows it's added to [dependencies], not [dev-dependencies]. Probably a mistake; trybuild should be a dev dep so it doesn't ship to consumers.~~ **Resolved (not a real issue): the trybuild line in commit d93df94 was added inside the existing `[dev-dependencies]` block; reviewer misread the diff hunk. Current Cargo.toml has it in `[dev-dependencies]`.**

  7. ~~IIR halfband has no denormal protection in its all-pass feedback path — oscen-lib/src/resample/halfband_iir.rs:40 (Allpass1::step). The cascade carries y_prev as IIR feedback, and BRANCH_B_BETAS[1] = 0.9056 is close to the unit circle. When the input decays toward zero (oscillator muted, gate released, between-note silence), the intermediate y_prev enters denormal range and on x86 each multiply can cost ~100× normal — audible CPU spikes / xruns. The FIR halfband doesn't share this issue (no feedback). Fix: add the standard anti-denormal trick at the end of Allpass1::step, e.g. let y = (y + 1e-30) - 1e-30;, or set DAZ/FTZ mtxcsr at process entry. The IIR variant is also the kernel users will reach for inside oversampled feedback loops, where denormals are most likely.~~ **Resolved in commits `dcedfd2` + `1d27833`. Snap-to-zero state protection in `Allpass1::step` (matching `iir_lowpass` pattern at threshold `1e-15`); `iir_hb_no_denormals_after_silence` and `iir_hb_down_no_denormals_after_silence` regression tests added. Round-trip verified: without the snap the cascade settles at `-6e-45` (up) and `-4e-45` (down) instead of exact zero.**

  8. ~~DSP / kernel correctness~~ **Resolved in commits `e4e1a59` + `4a3b8df` + `fcfac19`.**
  - ~~LinearUp::latency_samples() returns 1 regardless of N (linear.rs:30-32). Real impulse-response peak is at high-rate index N. Trait says "group delay at destination rate," so the value is wrong by a factor of N.~~ Fixed: returns `N`. Behavioral test `linear_up_impulse_peak_matches_latency` empirically verifies the peak position.
  - ~~LinearDown::latency_samples() returns 0 (linear.rs:62-64). It's a length-N moving average; group delay is (N-1)/2 source samples. Both reports are cosmetic today (graph latency_samples() only sums Down edges) but inconsistent with the trait contract.~~ Fixed: returns `(N-1)/2`. Docstring notes the `usize` truncation (N=2 → 0, true value 0.5).
  - ~~Cascade kernels only round-trip-tested at N=2 (tests/resample_kernels.rs). SincUpFir/SincDownFir/IirHalfband* cascade-state correctness for N=4/N=8 is uncovered — a stage-indexing regression would slip through.~~ Added `sinc_fir_passband_flat_n4`/`_n8` and `iir_hb_passband_flat_n4`/`_n8` round-trip tests. Polyphase-bug catch verified by experiment (corrupting `prev_odd_in` → max_err 0.75/0.88 vs threshold 0.1/0.15). Note: stage-order regressions inside an IIR halfband cascade are LTI-commutative and these tests cannot detect them — see new item #9 for the related coefficient-swap gap.

  9. IIR halfband round-trip passband tests (`iir_hb_passband_flat_n4` / `_n8` in tests/resample_kernels.rs, added in commit `4a3b8df`) do NOT detect a `BRANCH_A_BETAS` ↔ `BRANCH_B_BETAS` swap in `IirHalfband2x::new()` (halfband_iir.rs:74-81). Both arrangements yield valid (different) halfband filters that round-trip correctly — the round-trip-cancellation argument is independent of which set of betas sits in which polyphase slot. Defending against coefficient-array mix-ups specifically needs a stopband-shape test (e.g. assert attenuation at a specific stopband frequency exceeds N dB with the canonical coefficients). Surfaced during review of #8; left as a follow-up.
