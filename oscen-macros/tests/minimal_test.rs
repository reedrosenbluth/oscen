// Tests to verify param spec parsing works correctly
// The actual functionality is tested in oscen-lib integration tests

#[test]
fn test_parsing_compiles() {
    // Just verify the tests compile - the macro expansion happens at compile time
    assert!(true);
}
