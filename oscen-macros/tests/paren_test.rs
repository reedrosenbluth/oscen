// Test that the graph macro expands without errors
// The actual functionality will be tested in integration tests with oscen-lib

#[test]
fn test_macro_compiles() {
    // Just ensure the test itself compiles
    assert!(true);
}
